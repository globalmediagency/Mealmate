# CLAUDE.md — repères pour travailler sur MealMate

## Commandes

| Commande | Rôle |
|---|---|
| `npm run dev` | Serveur de développement (Turbopack) |
| `npm run build` | Build de production — doit passer **sans aucune variable d'environnement** |
| `npm run lint` | ESLint (config Next + TypeScript) |
| `npm run typecheck` | `tsc --noEmit` |
| `npm test` | Tests unitaires `vitest` (`lib/**/*.test.ts`) |

Avant chaque commit de fin de phase : `npm run build && npm run lint && npm test`.

## Contexte de travail du propriétaire

- Il travaille **100 % dans un navigateur** : GitHub, Vercel, Neon, Cloudflare R2, Google AI Studio, Stripe, Strava. Aucune commande locale, aucun `localhost`.
- Il teste sur les **URL de preview Vercel** (une par PR) puis en production après merge sur `main`.
- Chaque PR doit contenir : ce qui est testable sur la preview + une **checklist des actions manuelles** (variables Vercel, SQL à coller dans Neon, réglages dashboards).
- Interface en **français** ; code, identifiants et commentaires en **anglais**.

## Conventions

- **Jamais** de lecture d'env au niveau module pour créer un client : utiliser `requireEnv()` dans une fonction `getXxx()` mise en cache (`lib/db/index.ts`, `lib/auth/index.ts` sont les modèles). Une variable manquante lève `ConfigError` → 503 JSON dans les routes (`handleRouteError`) ou bandeau `ConfigBanner` dans les pages (`safeGetSession`).
- Pages qui lisent l'env ou la session : `export const dynamic = "force-dynamic"`.
- Routes API : `try { … } catch (e) { return handleRouteError(e) }`, entrées validées avec `zod`, réponses via `ok()` / `fail(code, message, status)`.
- Toute requête de données filtre sur `session.user.id`.
- Schéma : modifier **à la fois** `lib/db/schema.ts`, un nouveau fichier `db/migrations/NNN_description.sql` idempotent (`ADD COLUMN IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`…) **et** l'entrée correspondante de `lib/db/migrations-catalog.ts` (`checks` = tables / colonnes que le code lit, `sql` = les mêmes instructions ; `lib/db/migrations.test.ts` vérifie que les deux sont identiques). `db/init.sql` reste la photo complète du schéma courant : le mettre à jour aussi. Au premier rendu, `checkSchema()` (`lib/db/schema-check.ts`, layouts `(app)` et `admin`) compare la base au catalogue et affiche `SchemaOutdatedScreen` avec le SQL à copier si une migration manque ; les routes renvoient `schema_outdated` (503) sur une erreur Postgres 42703 / 42P01.
- Constantes de jeu : uniquement dans `lib/game/config.ts`. Logique de jeu pure (tick, tirages, effets) dans `lib/game/*.ts` avec un test `*.test.ts` à côté.
- Règles réglables depuis `/admin` : les fonctions pures prennent `rules: GameRules` (défaut `DEFAULT_RULES`) ; côté serveur, toujours charger `await getGameRules()` (`lib/game/rules-service.ts`) et le passer explicitement (`applyTick`, `toCreatureView`, `mealEffects`, `hatchProgress`). Ne jamais lire `TIER_CONFIG` pour une valeur numérique à l'exécution, seulement pour les libellés.
- UI : composants dans `components/`, classes Tailwind avec `cn()`. Tokens de couleur : `ink-*`, `sage-*`, `brass-*`, `cream-*`, `health`, `hunger`, `mood`, `danger`. Titres en `font-display`.
- Accessibilité : boutons ≥ 44 px, champs `text-base`, `aria-*` sur les icônes décoratives, animations coupées par `prefers-reduced-motion` (déjà global dans `globals.css`).
- Textes utilisateur : ton bienveillant, tutoiement, jamais culpabilisant.

## Ajouter une espèce

1. Ouvrir `lib/creatures/species/<tier>.ts` et ajouter un objet `Species` : `id` préfixé par le niveau (`facile-…`), `rarity`, `name` unique, `tagline` (une phrase, ton bienveillant), `palette` (`primary`, `secondary`, `accent`, `eye`), `parts` (`body`, `ears`, `eyes`, `mouth`, `tail`, `markings`, `extra`), `anchors` (`DEFAULT_ANCHORS[body]` sauf besoin particulier), `signature` (objet porté au stade Sage). Les `extra` (ailes, halo, cristaux, flammes, aura) sont réservés aux très rares / légendaires. Quota par niveau : 9 communs, 6 rares, 4 très rares, 1 légendaire.
2. Si une nouvelle part est nécessaire : ajouter le type dans `lib/creatures/types.ts`, le dessin dans le fichier de `components/creatures/parts/` concerné (coordonnées relatives à l'ancre, viewBox 100×100, corps ancré sur la ligne de sol y≈92), et si besoin une nouvelle silhouette dans `components/creatures/layout.ts`.
3. Vérifier dans `/dev/creatures?species=<id>` (`NEXT_PUBLIC_DEV_GALLERY=true`) sur les 4 stades × 4 états + silhouette. Les animations n'utilisent que `transform` avec `transform-box: fill-box`.
4. Lancer `npm test` : `lib/creatures/species.test.ts` vérifie quotas, unicité et réservation des extras.

## Collection Zodiaque (pixel art)

- Les douze animaux du zodiaque chinois (`lib/creatures/species/zodiaque.ts`, ids `moyen-zodiaque-<animal>`) forment une **collection** (`Species.collection = "zodiaque"`, `COLLECTIONS` dans `config.ts` : niveau `moyen`, quotas propres 6 / 3 / 2 / 1) ajoutée au roster du niveau : ils sortent des mêmes œufs Moyen (`speciesForTier` les place après les 20 espèces de base ; `speciesOfCollection()`), avec les poids de rareté habituels. Ils sont dessinés en **pixel art** : `Species.pixel` = une grille `PixelArt` de 28 × 28 caractères (`lib/creatures/pixel.ts` : `.` vide, `0`–`3` les quatre tons du plus sombre au plus clair, `E` cellule d'œil, `M` cellule de bouche ; ligne 26 posée sur le sol y = 92 ; `crown` = ligne du sommet du crâne pour les chapeaux, `neck` = ligne du cou), lue par `parsePixelArt()` (runs par ton, yeux regroupés avec leur reflet, bouche, ancres calculées par `pixelAnchors()`), une palette monochrome par état (`PIXEL_PALETTES`, silhouette `PIXEL_SILHOUETTE`) et une échelle par stade (`pixelStageScale`). Rendu 2D `components/creatures/pixel-creature.tsx` (`Creature` y bascule dès que `species.pixel` existe : rectangles nets `shape-rendering: crispEdges`, paupières `mm-lid` partagées avec les dessins, yeux croisés à la mort, étincelles pixel au stade Sage, accessoires aux ancres, `pixelLayout()` pour les effets communs, `yaw` = rétrécissement et miroir) ; rendu 3D `components/ar/three/pixel-mesh.ts` (voxels : une boîte par run, fusionnées par ton, yeux et bouche en relief, accessoires posés à plat via `accessory-attach.ts`, module partagé avec `creature-mesh.ts`). Les `parts` restent renseignées comme repli mais ne sont pas dessinées. Ajouter un animal = une entrée dans `zodiaque.ts` (le test vérifie la grille et les quotas de la collection) ; contrôle sur `/dev/creatures?species=<id>` et `/dev/screens?screen=creature-3d&species=<id>`.

## Pages de validation visuelle

- `/dev/creatures` : galerie espèces × stades × états, œufs et décors.
- `/dev/screens?screen=…` : écrans du jeu avec données factices (`egg`, `incubation`, `ready`, `reveal`, `home`, `home-sick`, `home-hungry`, `activity`, `feed`, `feed-animation`, `food`, `schema`, `ar`, `turnaround`, `creature-3d`, `defense`, `arena`, `arena-rtc`, `arena-invite`, `coop`, `pingpong`, `food-3d`, `admin-players`, `meal-result`, `meals`, `mourning`, `admin`, `play` (le hub des jeux), `play-catch`, `play-empty`, `wardrobe`, `chest`, `collection`, `friends`, `shop`, `home-protected`, `account`, `home-away`, `home-hosting`, `pension`, `mourning-pension`, `coach`, `coach-meals`, `home-pending`).
- `/dev/creatures?compact=1` : les 72 espèces (60 dessinées + les 12 du Zodiaque en pixel art) en un coup d'œil ; la page complète montre les 30 accessoires. `/dev/gemini` : modèles Gemini visibles avec la clé.
- `/admin` (hors galerie) : espace d'administration protégé par `ADMIN_USERNAME` / `ADMIN_PASSWORD` (aussi acceptés sur la page de connexion normale ; l'identifiant est réservé via `isAdminIdentifier()`), avec les onglets Règles de jeu, Créatures (`/admin/creatures`, filtres niveau / rareté, fiche stades × états, bouton cube `Species3dButton` sur chaque carte et chaque fiche : le volume 3D de « Voir en vrai » dans une boîte de dialogue, à faire tourner du doigt ou au clavier, stade et état au choix, via `Creature3dView` + `Turntable` de `components/ar/three/`), Accessoires (`/admin/accessoires`, par emplacement, aperçu porté à chaque stade) Aliments (`/admin/aliments`, les dessins de l'animation de nourrissage par famille, avec un testeur de nom d'aliment) Réseau (`/admin/reseau` : état de la case WebRTC et du relais Cloudflare TURN côté serveur, puis `TurnDiagnostic` (`components/admin/turn-diagnostic.tsx`) qui, **depuis le navigateur de l'admin**, demande des identifiants frais via `GET /api/admin/turn` (session admin, `getTurnIceServers({ fresh: true })`, identifiants inclus), collecte les candidats ICE (`summarizeCandidates`, `lib/arena/rtc-diagnostic.ts`), relie deux connexions de test dans la page avec `iceTransportPolicy: "relay"` puis `"all"` (temps de connexion, aller-retour, chemin via `selectedPairKind`), et rappelle l'état de la règle ; à lancer depuis le téléphone qui pose problème) et Joueurs (`/admin/joueurs` : tous les profils avec email et code ami, la créature active ou la dernière morte avec ses statistiques calculées en mémoire par `applyTick` sans rien écrire, ses caractéristiques, les compteurs repas / pas / amis / cimetière, la situation de pension, une recherche `?q=` via `matchesPlayer()`, et le marqueur imprimé servi par `GET /api/admin/marker?creature=<id>&format=svg|pdf`, attribué à l'ouverture si besoin ; données dans `lib/admin/players.ts`, carte `components/admin/player-card.tsx`, aperçu `/dev/screens?screen=admin-players` ; **actions sur le compte** (`components/admin/player-actions.tsx`, service `lib/admin/accounts.ts`) : « Lien de nouveau mot de passe » (`POST /api/admin/players/:id/reset-link` → `createPasswordResetLink()` : Better Auth `requestPasswordReset` dont le hook `sendResetPassword` remet le jeton à l'appelant via `captureResetToken()` de `lib/auth/reset-link.ts` (AsyncLocalStorage, MealMate n'envoie aucun email ; la route publique `/request-password-reset` est dans `disabledPaths`), lien `<origine>/reset-password?token=…` valable `RESET_LINK_HOURS` = 24 h, à usage unique, `revokeSessionsOnPasswordReset` ferme les autres sessions ; la page `/reset-password` (`components/auth/reset-password-form.tsx`, `authClient.resetPassword`) crée aussi un mot de passe à un compte Google) et « Supprimer le compte » (`DELETE /api/admin/players/:id` → `deletePlayerAccount()` = `deleteAccount()` de `lib/account/service.ts`, pseudo à retaper ; test `lib/admin/accounts.integration.test.ts`)).
- Les deux sont gardées par `NEXT_PUBLIC_DEV_GALLERY=true` (lue au build : redéployer après l'avoir changée).

## Écran Créature, navigation et hub Jouer

- **Écran Créature** (`components/game/creature-home.tsx`) : ce qu'on fait chaque jour, au-dessus de la ligne de flottaison. Ordre : en-tête (nom, sous-titre « espèce · rareté · stade · âge », seule pastille « Protégée n j ») → encarts (cadeaux, `CareAlert` = la ligne « raison » avec son action à droite) → scène `min(44vh, 360px)` (bulle à gauche, puce 44 px « Voir en vrai » → `/ar` à droite, créature = « Caresser ») → **rangée fixe** Nourrir · Jouer · Habiller (+ « Soigner (n) » seulement si la créature n'est pas en forme ou si une dose est en stock) → ligne « Aujourd'hui » (pas du jour + prochain coffre → `/activity`, ou « n coffre(s) à ouvrir » en laiton qui déplie `ChestOpener` sur place) → jauges compactes (`Gauge compact`, une ligne chacune, `moodHelp` derrière « Humeur ? »). Une seule tuile en laiton, décidée par `careAction(creature, doses)` (`lib/game/dialogue.ts`, testé) : `heal` (malade ou fatiguée avec une dose), `feed` (faim ≥ 60, ou malade sans dose), sinon « Jouer » quand il reste des parties. Sous-libellés : repas restants (`countMealsToday` / `rules.feeding.maxMealsPerDay`) et parties restantes (`countPlaysToday` / `rules.play.maxPerDay`), pastilles sur « Jouer » (invitations en direct, `useLiveArena`) et « Habiller » (accessoires reçus non vus). « Soigner » déplie `HealPanel` (`components/game/heal-panel.tsx`, `POST /api/inventory/use`, `creatureId` seulement pour une créature hébergée ; `/home#soigner` l'ouvre d'office) : partagé avec `PensionHome`. `HomePage` passe `chest`, `todaySteps`, `inventory`, `playsLeft`, `mealsToday`. Les créatures hébergées passent **avant** la propre créature en bande compacte (`HostedCreatures variant="strip"`). Plus de tuiles Défendre / Marcher / Arène ni de rangée RA.
- **Hub Jouer** (`/play`, `app/(app)/play/page.tsx` → `components/game/play-hub.tsx`, présentationnel) : compteur partagé en tête (`playsLeftLabel` + `playLimitLabel`, `lib/game/dialogue.ts` : la **seule** formulation de la limite, partout), invitations et partie en cours (`listMatchesFor`), cinq cartes — Attrape-repas (`/play/catch`, ex-`/play`), Défendre (`/defense`), Défendre ensemble (`/arena?mode=coop`), Arène (`/arena?mode=arena`), Ping-pong (`/arena?mode=pingpong`) — avec ce qu'il faut (solo / entre amis, « RA »), atténuées mais ouvrables à 0 partie ; note quand aucun ami n'a de créature vivante ; ligne marqueur compacte (`MarkerCard compact`, `ensureCreatureMarker` pour sa propre créature dans le `Promise.all`, jamais pour une hébergée) ; ligne « Voir en vrai » (pas un jeu) ; `<details>` RA. `?creature=<id>` = créature hébergée (solo seulement, retour → `/pension/<id>`). Les jeux reviennent au hub (`homeHref` = `/play`, chevron « Retour aux jeux », bouton de fin « Autres jeux ») ; `/arena` lit `?mode=` (`isArenaMode`, `lib/arena/labels.ts` = `ARENA_MODE_LABELS`, les noms des modes partout : « Défendre ensemble » remplace « Défendre à deux ») et `ArenaList` reçoit `initialMode` (sélecteur masqué, « Changer de jeu ») et `playsLeft`.
- **Navigation** : `PageHeader back={{ href, label }}` = chevron 44 px à gauche du titre (« Retour à ma créature », « Retour aux jeux », « Retour au coaching », « Retour ») sur toute page hors onglet ; `BottomNav` allume l'onglet parent via `PARENT` / `tabFor()` (`/feed`, `/play`, `/defense`, `/arena`, `/ar`, `/wardrobe`, `/shop`, `/pension`, `/collection`, `/cemetery` → Créature ; `/coach` → Amis ; `/privacy`, `/legal` → Plus) ; les invitations en direct comptent sur l'onglet **Créature** (plus Amis), `FriendsPanel` garde une ligne « n invitation(s) à jouer → /play ». Page Plus = annuaire court (Ma créature, Jouer, Soins, Mon compte, À propos), « État des services » replié en `<details>` en bas, alerte joueur si Gemini / R2 / Stripe manquent. `safe-top` **ajoute** 0,75 rem à l'encoche (plus de `pt-3` dans les layouts).

## Ajouter un accessoire

1. Ajouter l'entrée dans `lib/accessories/catalog.ts` (`id`, `name`, `slot` ∈ `head | eyes | neck | body`, `rarity`, `tagline`).
2. Dessiner le SVG dans le fichier de `components/accessories/` du slot (`head.tsx`, `eyes.tsx`, `neck.tsx`, `body.tsx`) : un objet `{ front?, back? }` dont les coordonnées sont relatives à l'ancre (tête = sommet du crâne, y vers le bas ; yeux = centre de la ligne des yeux, verres à x = ±9 ; cou = base de la tête ; corps = centre du corps). `back` est dessiné derrière le corps (cape, ailes, sac).
3. Contrôler sur `/dev/creatures` (section « Accessoires » + « Portés ») sur plusieurs silhouettes et stades. Le test `lib/game/accessories.test.ts` vérifie qu'un rendu existe pour chaque entrée du catalogue.

## Mini-jeu et coffres

- Le jeu (`components/game/food-catch-game.tsx`) ne passe pas par l'état React à chaque frame : positions écrites directement dans `style.transform` depuis `requestAnimationFrame`, éléments recyclés (pool). Garder cette discipline pour rester à 60 fps sur mobile.
- Le serveur recalcule le score (`computePlayScore`) à partir des compteurs bruts : ne jamais faire confiance à un score client. `recordPlay(…, rules, kind)` enregistre les cinq jeux (`play_sessions.kind` ∈ `catch | defense | arena | coop | pingpong`, migrations 014, 015, 016 et 018) dans la même limite quotidienne par créature, `rules.play.maxPerDay` (règle admin, défaut `PLAY.maxPerDay`) : ne plus lire `PLAY.maxPerDay` ailleurs que comme défaut, les pages passent `maxPerDay` aux composants de jeu.
- Coffres : `getChestStatus(creature)` (somme des pas depuis l'éclosion − `accessory_drops`), `openChest()` réserve le coffre par `UPDATE` conditionnel avant le tirage. Le tirage (`drawAccessory`, `drawSpecies`) est un jet pondéré sur tout le groupe via `lib/game/drops.ts` ; les surcharges admin viennent de `getDropWeights()` (`lib/game/drops-service.ts`), à passer explicitement. Les poids sont des pourcentages du groupe quantifiés à 0,001 % (`quantizeWeight`, `WEIGHT_DECIMALS`), le document stocké porte `unit: "percent"` (sans unité = ancien format en millièmes, lu ÷ 10), une surcharge égale au défaut n'est pas stockée, un groupe entièrement à 0 est refusé à l'enregistrement (`empty_pool`) et, s'il arrivait quand même en base, `effectiveWeights()` retombe sur les défauts : affichage admin et tirage partagent ce repli.
- Exemplaires : `user_accessories.qty`. Toujours passer par `addAccessoryCopies()` / `takeAccessoryCopy()` (`lib/accessories/service.ts`) pour déplacer un accessoire (trocs, dons, coffres) : jamais d'`INSERT` / `DELETE` direct, la prise du dernier exemplaire retire l'accessoire des tenues. Sans transaction, un échange à deux sens prend d'abord les deux exemplaires (décréments conditionnels), rembourse et annule si le second manque, puis seulement les redistribue (`acceptTrade`).

## Pension

- Une créature vivante et nommée peut être confiée à un ami accepté (table `boardings`, migration 009). Trois règles admin (`GameRules`) : durée maximale **par niveau** (`tiers[tier].boardingMaxDays`, défaut 30 j, `boardingLimits()` donne les durées proposées), `boarding.maxPerHost` (défaut 5, 0 désactive) et `boarding.cooldownMultiplier` (défaut 1 : après X jours de pension effectifs, le propriétaire attend X × ce facteur avant de confier à nouveau, `boardingCooldownUntil()`, erreur `boarding_cooldown`). Toujours passer `rules` à `startBoarding`. `startBoarding` crée une **proposition** (`status = pending`, migration 011) : la créature reste chez son propriétaire (`getHeldCreatures().proposal`) jusqu'à `respondToBoarding(hostId, id, accept)` qui démarre le séjour à l'instant de l'acceptation (`active`, capacité revérifiée) ou le décline ; `endBoarding` sur une proposition = `cancelled` (propriétaire) / `declined` (hôte). L'hôte voit les propositions sur son écran Créature (`listProposalsFor`, pastille), le propriétaire voit réponses et retours anticipés (`listOwnerNotices`, `owner_seen_at`). Propositions déclinées ou retirées : ni garde des pas, ni repos. Une seule pension ouverte (proposée ou active) par créature (index unique partiel).
- **Toujours** raisonner en « créatures dont on s'occupe » : `getHeldCreatures(userId)` (`lib/boarding/service.ts`) renvoie `own` (tick appliqué), `away` (sa propre créature en pension ailleurs) et `boarded` (celles hébergées, tick appliqué). `livingHeld()` = celles que nourrissent les repas et les pas. Pour une action sur une créature précise (jeu, soin, coffre) : `getHeldCreature(userId, creatureId?)`, qui refuse la créature partie en pension (`creature_boarded`) et les créatures d'autrui (`not_found`) ; passer `{ boarded: true }` à `recordPlay`, `consumeMedicine`, `openChest` (`assertHolder`).
- La fin de pension est paresseuse (`settleBoarding` à chaque lecture) : mort → `died` (remet `host_seen_at` à `NULL` : l'hôte acquitte l'encart de décès via `markBoardingsSeen(hostId, now, boardingId)`, le propriétaire voit « pendant sa pension chez X » dans le deuil via `diedInBoarding()` ; un décès ne compte jamais pour le repos), échéance → `expired` ; `endBoarding()` pour `recovered` (propriétaire) / `returned` (hôte), par `UPDATE` conditionnel.
- Pas et coffres : les pas d'un jour vont au **détenteur** de ce jour (`lib/boarding/custody.ts`, jour de départ à l'hôte, jour de retour au propriétaire) ; `getChestStatus()` passe par `creatureStepsSince()`. L'hôte garde les accessoires des coffres ouverts pendant la pension ; il ne peut pas changer la tenue (`equipAccessory` reste réservé au propriétaire).
- Un repas nourrit toutes les créatures vivantes détenues (`feedCreature` → `others`), avec des effets calculés par créature (niveau, faim). La limite de repas par jour reste par joueur ; la limite de parties est par créature (`countPlaysToday(creatureId)`).

## Coaching

- Un joueur choisit son coach parmi ses amis acceptés (`proposeCoach`, table `coachings`, migration 010) ; le coach accepte ou décline (`respondToProposal`), chacun peut mettre fin à tout moment (`endCoaching` : `cancelled` / `declined` / `ended` + `ended_by`). Un seul coaching `pending` ou `active` par élève (index unique partiel) ; un coach peut avoir plusieurs élèves.
- Le coach voit les repas de son élève (photos signées, date et heure, note de l'appli) via `studentMealsForCoach()` : **seule** exception à la règle « photos jamais montrées à un autre utilisateur », réservée à la relation `active`. Il pose un pouce par repas (`reviewMeal`, table `meal_reviews`, un avis par repas, modifiable) ; les compteurs `thumbs_up` / `thumbs_down` vivent sur `coachings` et survivent à la purge des repas.
- Récompenses (`rewardStatuses`, `openCoachingReward`) : élève = Σ max(verts − rouges, 0) sur tous ses coachings ÷ `coaching.thumbsPerStudentReward` (règle admin, défaut 5) ; coach = Σ (verts + rouges) sur tous ses coachings ÷ `coaching.thumbsPerCoachReward` (défaut 10). Compteurs `profiles.student_rewards_opened` / `coach_rewards_opened` réservés par `UPDATE` conditionnel. Tirage sur `rewardPool(role)` = accessoires des coffres + ceux réservés au rôle (`Accessory.source` ∈ `chest | student | coach`, `CHEST_ACCESSORIES` pour les coffres classiques) ; un accessoire garde le même poids dans tous les groupes (part de sa rareté dans le groupe des coffres), seul le total change.
- Conservation des repas : `purgeExpiredMeals(userId, storage, now, rules.feeding.mealRetentionDays)` (règle admin, défaut 30 j) supprime photo R2 puis ligne, appelée paresseusement avant toute lecture d'historique (page Repas, `GET /api/meals`, vue du coach). Une photo impossible à supprimer garde sa ligne.
- Écrans : `/coach` (onglet « Coaching » à côté d'« Amis », `SocialTabs`), `/coach/[id]` pour le coach ; pastille de l'onglet Amis = propositions reçues + réponses non vues (`countCoachingBadges`, `POST /api/coaching/seen`).

## Amis

- Tout ce qu'un ami peut voir passe par `FriendCreatureView` (`lib/friends/service.ts`) : ne jamais renvoyer une `CreatureView` complète ni un profil complet (code ami, email) à un autre utilisateur.
- Les lectures de créatures d'amis appliquent le tick (`tickCreature`) : c'est voulu, l'état affiché doit être le vrai.

## Boutique, soins et trocs

- Prix et libellés des soins : `SHOP_ITEMS` dans `lib/game/config.ts` ; effets purs dans `lib/game/medicine.ts` (`applyMedicine`, `needsCare`) avec test à côté.
- Stripe : client paresseux `getStripe()` (`lib/payments/stripe.ts`). Le service `lib/shop/service.ts` reçoit un `CheckoutProvider` injecté (`stripeProvider` en prod, faux fournisseur dans `lib/shop/shop.integration.test.ts`). Toujours créditer via `creditPurchase()` (idempotent) : jamais d'`INSERT` direct dans `inventory` depuis une route.
- Le webhook `app/api/webhooks/stripe/route.ts` lit le corps **brut** (`request.text()`) avant `constructEvent` : ne pas le parser en JSON avant.
- Décrémenter l'inventaire uniquement par `UPDATE … WHERE qty > 0` (`consumeDose`).
- Soins aux amis : passer par `healFriendCreature()` (ami accepté, créature vivante et `needsCare`), qui applique le tick avant d'agir et journalise dans `gifts`.
- Trocs et dons : `lib/trades/service.ts`. L'acceptation bascule d'abord le statut par `UPDATE` conditionnel puis déplace un exemplaire dans chaque sens. `giftAccessory()` donne un exemplaire sans acceptation et journalise dans `gifts` (`kind = 'accessory'`).

## Strava

- Client dans `lib/strava/api.ts` (interface `StravaApi`, implémentation `stravaApi` avec `requireEnv` à l'appel) ; le service `lib/strava/service.ts` reçoit l'API injectée, les tests (`strava.integration.test.ts`) utilisent une fausse API.
- Conversion activité → pas et fenêtre / throttle : `lib/game/strava.ts` (constantes dans `STEPS.strava`), test à côté.
- Le `state` OAuth est signé (`lib/strava/state.ts`) et vérifié contre `session.user.id` dans le callback : ne jamais accepter un callback sans session ni `state` valide.
- Les jetons ne sortent jamais du serveur : renvoyer `StravaStatus`, jamais la ligne `strava_connections`.
- Import idempotent par `ON CONFLICT (strava_activity_id) DO NOTHING` ; crédit à la créature **par jour toutes sources** via `creditDays()` (jamais par activité).
- Le bouton « Connecter Strava » est une balise `<a href="/api/strava/connect">` (pas `Link`) : la route répond par une redirection externe.

## Compte (export, suppression)

- `lib/account/service.ts` : `exportAccount()` (jamais de clé d'image ni d'identifiant d'un autre utilisateur, seulement des pseudos) et `purgeExternalData()` (préfixe R2 `meals/<userId>/` + révocation Strava). La suppression passe par `deleteUser` de Better Auth (`lib/auth/index.ts`, hook `beforeDelete`) ; tout le reste est en `ON DELETE CASCADE` : toute nouvelle table liée à un utilisateur **doit** référencer `"user"(id) ON DELETE CASCADE` et être ajoutée à `countUserFootprint()` pour le test.
- En-têtes de sécurité dans `next.config.ts` ; pages privées en `robots: noindex` via les layouts `(app)`, `admin`, `dev`.
- Accessibilité : cibles ≥ 44 px (`min-h-11` / `h-11 w-11`, avec marge négative pour les icônes de fermeture), focus global dans `globals.css`, lien d'évitement `.skip-link` dans le layout `(app)`.

## Humeur

- Effets purs dans `lib/game/mood.ts` (`moodBand`, `applyMoodToXp`, `chestBonusSteps`, `gloomyHours`), seuils dans `rules.mood` (`MOOD` de `config.ts` = défauts, réglables depuis `/admin`). Toujours passer l'humeur **d'avant l'action** : `mealEffects({ mood })`, `playEffects(score, mood, rules)`, `applyStepGains(creature, gains, rules)` (les `gains` portent `steps`, les pas nouvellement crédités). Le tick applique la perte de santé « triste ». Les pas bonus de coffre s'accumulent dans `creatures.chest_bonus_steps` (migration 012) et sont ajoutés par `getChestStatus()`. La vue expose `moodBand` + `moodEffects` (`moodEffectsFor`) pour les libellés `moodLabel()` / `moodHelp()` de `lib/game/dialogue.ts`.

## Voir en vrai (réalité augmentée)

- **Un marqueur par créature** : `creatures.ar_marker` (migration 013), numéro AprilTag 36h11 (587 codes, `AR_MARKER.ids`) attribué à la première demande par `ensureCreatureMarker()` (`lib/ar/service.ts` : tirage `pickMarkerId()` parmi les numéros que ni le propriétaire ni ses amis acceptés n'utilisent, `UPDATE … WHERE ar_marker IS NULL`). `listArTargets(userId)` = tout ce que la caméra du joueur peut reconnaître : sa créature (même en pension ailleurs), celles qu'il héberge, celles de ses amis acceptés qui ont déjà un marqueur ; une seule cible par numéro (priorité propre > hébergée > amis, les autres dans `conflicts`). `GET /api/ar/marker?creature=<id>` (session, créature du joueur uniquement) renvoie le PDF avec le nom sous le carré ; la carte `MarkerCard` (`components/ar/marker-card.tsx`, aperçu + PDF pour sa propre créature) est affichée sur « Voir en vrai » et sur « Défendre ».
- `lib/ar/config.ts` (`AR_MARKER` : dictionnaire, nombre de codes, taille imprimée, sans dépendance, importable côté client), `lib/ar/marker.ts` (`markerCells(id)`, `markerSvg(id)`), `lib/ar/pdf.ts` (`markerPdf({ id, name })`, pdf-lib), `lib/ar/geometry.ts` (pure : `markerPose`, `coverTransform`, `mapQuad`, `smoothPose`, tests à côté). Le détecteur est js-aruco2 intégré dans `lib/ar/vendor/` (MIT ; `apriltag_36h11.js` sous licence BSD enregistre le dictionnaire, à importer avant de créer un `Dictionary` ou un `Detector` ; seul l'en-tête des fichiers a changé pour exporter en ES ; ignoré par ESLint ; ne pas réinstaller le paquet npm, son export via `this` est perdu au bundling) et chargé à la demande par `loadAruco()`.
- `components/ar/ar-viewer.tsx` reçoit `targets: ArTarget[]` (numéro → créature, `mine`, `ownerName`) : caméra, boucle de détection sur une copie réduite de l'image, étiquette « nom · propriétaire » par marqueur reconnu positionnée par `style.transform` (l'état React ne change qu'à l'apparition ou la disparition d'un marqueur), photo composée de la vidéo et des créatures visibles, arrêt quand l'onglet est caché. **Niveau 3 (vraie 3D, `mode = "three"`)** : un canvas WebGL (`object-fit: cover`, tampon de la taille de la vidéo) recouvre la vidéo ; `ThreeStage` (`components/ar/three/stage.ts`, chargé à la demande) tient une `PerspectiveCamera` dont le champ de vision vient de `cameraFovDeg()` (focale supposée = largeur de l'image), un groupe par marqueur (repère du papier : x à droite, y vers le bord haut, z hors du papier) placé par `posesFromCorners()` + `pickPose()` (`lib/ar/pose3d.ts`, POSIT coplanaire de `vendor/posit1.js`, conversion en rotation propre vers les axes Three.js, choix de la solution la plus proche de la précédente) puis lissé (slerp / lerp 0,35, saut à la première apparition), un disque d'ombre dans le plan du papier et la créature debout (`quaternion = Rx(+90°)` : son haut = z du papier, elle regarde le bord bas, où le nom est imprimé). Le même volume s'affiche seul, sans marqueur, dans `Creature3dView` (`components/ar/three/creature-3d-view.tsx`, caméra en orbite de `Turntable`, `turntable.ts`, rotation automatique coupée au premier glisser, secours = dessin 2D) : c'est le bouton 3D de l'onglet admin Créatures et l'écran `/dev/screens?screen=creature-3d`. Les SVG d'accessoires cachés sont rendus par `AccessorySprites` et relus par `accessoryMarkup()` (`accessory-sprites.tsx`), la rastérisation par `textureFromSvg()` (`textures.ts`). Les créatures sont construites par `buildCreatureMesh()` (`components/ar/three/creature-mesh.ts`) à partir des mêmes pièces que le dessin 2D (`LAYOUTS`, `stageScales`, palette teintée par l'état) : sphères et ellipsoïdes pour le corps et la tête, et **les tracés SVG du dessin 2D eux-mêmes** extrudés en plaques fines (`svg-shape.ts` : `shapesFromPath()` lit toute la grammaire M / L / H / V / C / S / Q / T / A / Z, absolue ou relative, en retournant y et marque `autoClose` sur les sous-tracés fermés, `pointsFromPath()` échantillonne une courbe pour un tube, `extrudeGeometry()` centre la plaque sur z = 0 ; le constructeur `flat()` empile les parties d'un même dessin, chacune un peu en avant de la précédente) pour les oreilles pointues (`EAR_PATHS`, mêmes triangles et courbes que `parts/ears.tsx`, penchées comme dessinées : rotation SVG = −rotation Three.js, oreille gauche en miroir `scale.x = −1` avec la rotation inversée), les ailes crème veinées (`WING`, qui battent comme `mm-flap`), les gemmes (`GEM`), la queue enflammée (`FLAME`, qui vacille comme `mm-flicker`), l'étoile des yeux `sparkle` et **l'objet signature du stade Sage** (`buildSignature` : couronne = bandeau + 4 pointes + 3 gemmes, feuille, fleur, monocle sur l'œil droit, nœud, écharpe, clochette, épingle étoile, perles, bandana ; même règle qu'en 2D : tête libre d'accessoire de tête / yeux, cou libre d'accessoire de cou / corps ; le halo monte au-dessus d'une couronne) ; cônes et tubes pour le reste ; hauteur `CREATURE_HEIGHT_UNITS` = 2,2 côtés de marqueur. Tout trait du visage (yeux, bouche dont les lignes épousent la surface via `depth`, joues, museau du marquage `mask`), les marquages de tête (`buildHeadMarkings` : traits du front `stripes`, blaze `mask`, bandes `badger`, croissant `crescent`), du corps (`belly_patch`, `spots`, `stripes` en traits qui épousent la surface, `scales` en petits arcs, absents chez les bébés et atténués chez les enfants comme en 2D) et de dos (`buildShell` : carapace derrière le corps avec ses hexagones ; `buildSpikes` : crête de cônes autour du haut de la tête) sont posés **sur** la surface du volume (`surfaceZ`, `bodySurface`, `frontZ`), jamais au centre : un élément à z = 0 est caché dans le corps. **Les accessoires sont de vrais volumes** construits à partir de leur propre SVG (`AccessoryLayerSvg`, rendu caché dans le viewer, relu par `accessoryMarkup()` et passé à `buildCreatureMesh` par `CreatureMeshInput.markup`) : `svg-solid.ts` (`solidFromSvg(markup, { depth, facing, drape })`) lit le dessin avec `DOMParser` (path, circle, ellipse, rect arrondi, line, polygon, groupes et leurs `transform` translate / scale / rotate / matrix, fill / stroke / opacités hérités, couleurs `rgba()`, `fill-rule="evenodd"` → trous), extrude chaque aire pleine en plaque fine (fond translucide des verres relevé à `GLASS_ALPHA`), transforme chaque ligne en tube (bouts ronds si `stroke-linecap="round"`), empile les éléments dans l'ordre du dessin (`step`, sens `facing` : +1 devant, −1 pour une couche de dos) et **drape** le tout sur la surface indiquée (`drape(x, y)` = z de la surface sous le point ; les plaques sont d'abord raffinées, `refineGeometry`, aucune arête plus longue que `DRAPE_MAX_EDGE`, pour épouser la courbure). Il renvoie aussi l'étendue, les points extrêmes et `frameColor` (couleur du trait le plus épais, sinon de l'aire principale). Dans `creature-mesh.ts` : lunettes et masques dans le plan du visage, légèrement cintrés sur la tête (`bentOnFace`, 40 % de la courbure) avec des **branches** (`buildTemples` : depuis les bords extérieurs du dessin, droit vers l'arrière puis le long de la tête jusque derrière les oreilles, en `frameColor` ; seulement si le dessin couvre les deux côtés, un monocle n'en a pas) ; colliers, écharpes et médailles drapés sur l'avant du corps (`frontZ`) ; tenues et capes drapées sur le ventre (`front`) et sur le dos (`back`, éléments suivants plus en arrière). Sans markup (ou `DOMParser`), retour aux anciens `Sprite` / cartes texturées (`textureFromSvg()`, `textures.ts`). Les chapeaux restent des `Sprite` face à la caméra : un chapeau se ressemble sous tous les angles, c'est leur meilleur volume à bas prix. Ces attaches vivent dans `accessory-attach.ts` (`createAccessoryAttacher`), partagé avec les voxels des espèces pixel (`pixel-mesh.ts`) ; `units.ts` porte `CREATURE_HEIGHT_UNITS`. Toute nouvelle pièce 2D (oreille, queue, bouche, marquage, extra, signature) doit recevoir son équivalent dans `creature-mesh.ts` (de préférence en réutilisant son tracé SVG), puis être contrôlée sur `/dev/screens?screen=creature-3d&species=<id>&stage=<stade>&acc=<ids d'accessoires séparés par des virgules>&yaw=<degrés>` (dessin 2D à gauche, volume à droite, tous deux tournés de `yaw` et portant les mêmes accessoires ; `window.__turntable` expose la scène aux sondes quand la galerie est active). Si WebGL manque (ou le module ne charge pas), le viewer retombe sur le niveau 2 (`mode = "views"`, `ViewsRenderer`, `view` de `viewFromAngle(pose.angle, précédent)`, `lib/creatures/turnaround.ts`, hystérésis de 6°). Changer le marqueur = changer `AR_MARKER` : PDF, aperçu et détecteur suivent, `lib/ar/pdf.test.ts` vérifie que le PDF reproduit la grille du détecteur.
- **Faux 3D (tour d'horizon, niveau 2, secours)** : `Creature` accepte `yaw` (degrés, 0 = face, positif = sens horaire vu de dessus, le visage glisse vers la gauche du spectateur). `turnPlacements(layout, yaw)` (`components/creatures/turn.ts`) projette chaque trait sur une sphère (`turnFeature` : x, `squash`, visibilité, profondeur) : yeux et joues par côté (l'œil du fond disparaît au profil, tout le visage de dos), bouche et lunettes (cachées de dos), oreilles (jamais cachées, retournées quand elles changent de côté, largeur ≥ 45 %), queue (passe devant le corps quand `depth > 0,1`, miroir à gauche), chapeau et collier décalés, accessoire de corps : couche `back` devant le corps quand `facing < 0`. `yaw = 0` donne exactement le rendu d'avant. Contrôle visuel : `/dev/screens?screen=turnaround` (5 silhouettes × 8 vues).
- Test sans caméra : `/dev/screens?screen=ar` affiche le viewer avec deux cibles (n° 17 et 42) et leurs aperçus ; Chromium accepte une fausse caméra (`--use-fake-device-for-media-stream --use-file-for-fake-video-capture=<fichier.mjpeg>`) construite à partir d'une capture de cet aperçu, posée en perspective (`perspective(900px) rotateX(50deg)`) pour voir la créature debout ; `--disable-webgl` force le repli sur les huit vues (`data-mode="views"`, `data-view`).

## Défendre (tower defense en réalité augmentée)

- Logique pure dans `lib/game/defense.ts` (test à côté), repère du papier (côté = 1, z vers le haut, créature à l'origine) : `createDefense(rules.defense)`, `startDefense`, `stepDefense(state, dt, random)` (mutation en place, `dt` en secondes, générateur injecté), `fireDefense(state, aim)` (rechargement, la créature se tourne : `state.yaw`), `endDefense`, **boss** (`bossesInWave`, `bossHitsFor` : un aliment géant surgit à un moment aléatoire de chaque vague multiple de `rules.defense.bossEveryWaves` (créneau tiré uniformément parmi les apparitions de la vague), échelle `DEFENSE.bossScale`, moitié moins vite, dégâts doublés, `hits` œufs à encaisser (+1 par boss rencontré), rayon de collision `bossRadius`, effet `hit` à chaque œuf encaissé, barre de vie en sprites au-dessus de lui dans `DefenseScene`), **bons aliments et langue** (`GOOD_KINDS` / `GOOD_FOODS` : fruits et légumes qui apparaissent pendant une vague, `state.bonuses`, restent `goodStaySeconds`, clignotent `goodBlinkSeconds` (`bonusBlinking`) puis disparaissent, écrasés par un œuf (`goodWasted`) ; `tongueDefense(state, aim)` : la langue sort vers le point visé jusqu'à `tongueMaxLength`, attrape à pleine extension (`tongueExtension`, `catchWithTongue`, couloir `tongueRadius`, hauteur `tongueHeight`) tout ce qui est sur le segment : bons aliments = soin plafonné à `rules.hp` (`goodEaten`, `healed`, effet `heal`), malbouffe = dégâts (`junkEaten`), les boss sont trop gros pour être avalés ; une langue à la fois, rechargement `tongueCooldownMs`), `computeDefenseScore(summary, rules)` côté serveur (compteurs bornés par `maxSpawnedThrough`). Les constantes fixes sont dans `DEFENSE` (`config.ts`), les six réglables dans `rules.defense` (section « Défendre » de `/admin`). Nouvel aliment ennemi : ajouter la clé (un `FoodKind` existant) dans `JUNK_KINDS`, sa fiche dans `JUNK_FOODS` (déplacement, dégâts, vague d'apparition) et son **modèle 3D** dans `MODELS` de `components/ar/three/food-mesh.ts` (nouveau bon aliment : `GOOD_KINDS`, `GOOD_FOODS` (soin) et le même `MODELS`, type `FoodModelKind`) (primitives Three.js, modélisé y-up debout sur y = 0, face vers +x, environ un demi-côté de marqueur, couleurs des dessins 2D ; `buildFoodMesh(kind)` le redresse dans le repère du papier, un gabarit par sorte cloné par aliment) ; contrôle visuel sur `/dev/screens?screen=food-3d` (`FoodGallery`).
- Rendu : `components/ar/marker-camera.ts` (`MarkerCamera` : caméra + détecteur, `onFrame({ markers, videoWidth, videoHeight, now, period })`, coins en pixels vidéo, `CameraError.kind` ∈ `unsupported | denied | error`, partagé avec le viewer ; copie de détection `DETECT_PIXELS` ≈ 300 000 pixels (largeur ≤ `DETECT_MAX_WIDTH` = 640, forme de la vidéo : un téléphone tenu droit livre un flux portrait au même coût), une image de caméra identique à la précédente (`video.currentTime`, caméra 30 fps sous une boucle 60 Hz) est sautée sauf si l'horloge ne bouge plus depuis `STALLED_CLOCK_MS`, une détection plus longue que `SLOW_DETECT_MS` (8 ms) est suivie d'un repos de même durée (`nextDetectAt`) : jamais plus de la moitié du fil principal ; `CameraFrame.period` = millisecondes depuis la détection précédente : le viewer cache une créature après `max(LOST_MS, 3 × period)`, les jeux gardent la dernière pose `max(HOLD_MS, 3 × period)` et simulent une image lente en sous-pas de `MAX_DT` (50 ms ; au plus `MAX_FRAME_SECONDS` = 0,5 s rattrapées, le reste est une pause : rien ne traverse la créature, aucun œuf ne saute son atterrissage ; en coop via `advanceDefense`)) ; `ThreeStage.setYaw`, `attach`, `aimOnMarker` (axe de la caméra ∩ plan du marqueur) ; `components/ar/three/defense-scene.ts` (`DefenseScene.sync(state, aim)` : aliments en volumes (`food-mesh.ts`) tournés vers la créature avec une ombre au sol, bons aliments qui tournent sur place et clignotent, langue = cylindre fin étiré depuis la **bouche de la créature** (`CreatureMesh.mouth` calculé par `buildCreatureMesh`, lu par `ThreeStage.mouthOf(id)` et passé par `DefenseScene.setMouth`, repli `tongueBaseHeight` / `tongueBaseOffset`) jusqu'à la pointe, œufs, fumée, éclats, flash, étincelles vertes de soin, anneau de visée, objets recyclés par sorte, jamais de mutation de l'état) ; `components/defense/defense-game.tsx` (phases `intro → starting → scanning → playing → submitting → done`, `paused` quand l'onglet est caché, marqueur perdu plus de `max(HOLD_MS 1,2 s, 3 détections)` = pause, HUD via `data-defense-hud` mis à jour seulement au changement, boutons `data-defense-fire` (œuf, espace / entrée) et `data-defense-tongue` (langue, touche L), `preview` pour les écrans de dev). Page `app/(app)/defense/page.tsx` (`getHeldCreature`, marqueur attribué par `ensureCreatureMarker` pour sa propre créature), route `POST /api/defense`.
- Test sans caméra : `/dev/screens?screen=defense` avec le marqueur n° 17 sous la zone de jeu ; même recette de fausse caméra que « Voir en vrai » (marqueur posé en perspective sous le centre de l'image pour que la visée tombe devant la créature).

## Arène (bataille entre amis en réalité augmentée)

- Une partie (`arena_matches`, migration 015 : `arena_matches`, `arena_players`, `arena_bonuses`, `arena_events`, et `play_sessions.kind` accepte `arena`) réunit 2 à `ARENA.maxPlayers` amis acceptés, chacun avec sa créature vivante et nommée sur son **propre marqueur** (`ensureCreatureMarker` pour tous à la création ; deux numéros identiques dans la même partie = `marker_conflict`). Les règles sont gelées dans la partie (`max_hp`, `egg_damage`, `duration_seconds` ← `rules.arena`, section « Arène » de `/admin` ; `rules.arena.webrtc` = souhait de transport, voir plus bas). Service `lib/arena/service.ts` : `createMatch(hostId, friendIds, now, rules)` (hôte `ready`, amis `invited`, `arena_busy` si une partie est déjà ouverte, `play_limit` vérifié), `respondToInvite`, `startMatch` (≥ 2 prêts ; les invités sans réponse passent `declined`), `leaveMatch` (l'hôte qui quitte une salle l'annule ; en bataille = `left`, compté comme sorti), `cancelMatch`, `snapshot(userId, matchId, now, rules, { since })` avec **tick paresseux** (salle périmée après `lobbyTtlMinutes` ; bon aliment dû : `next_bonus_at` réservé par `UPDATE` conditionnel puis `placeArenaBonus` (`lib/game/arena.ts`) ancre le fruit à une créature debout ; fin quand le temps est écoulé ou qu'une seule créature tient debout : `finishIfOver` bascule le statut par `UPDATE` conditionnel et pose les rangs de `rankArenaPlayers` ; récompense du lecteur réservée par `rewarded_at` puis `recordPlay(…, "arena")` avec `arenaScore` (moitié santé restante, moitié rang), stockée dans `reward` jsonb : `{ score, perfect, effects, playsLeft }` ou `{ skipped: left | play_limit | … }`), `recordShot` (cadence `shotMinIntervalMs` par `UPDATE … WHERE last_shot_at < …`, dégâts par `UPDATE … WHERE hp > 0`, événements `egg` / `eliminated`), `eatBonuses` (chaque bon aliment mangé une seule fois par `UPDATE … WHERE eaten_by IS NULL AND expires_at > now`, soin plafonné dans le même `UPDATE`, événement `tongue`), `listMatchesFor`, `countArenaInvites` et `listArenaInvites` (invitations en attente, sans écriture). **Invitations en direct** : `LiveArena` (`components/arena/live-arena.tsx`, monté dans le layout `(app)`) interroge `GET /api/arena/notices` toutes les `ARENA.notices.pollMs` (5 s ; 30 s onglet caché ; aussitôt au retour de l'onglet ou du focus ; repli exponentiel sur erreur), affiche une carte `ArenaInviteToasts` au-dessus de la barre basse sur **toutes** les pages (« Voir la partie » / « Plus tard », rejet mémorisé en `sessionStorage`, jamais sur la page de la partie elle-même), fournit le compte à `BottomNav` via `useLiveArena()` (le layout serveur ne compte plus les invitations dans la pastille Amis, pour ne pas doubler), rafraîchit `/arena` et `/friends` à l'arrivée d'une nouvelle invitation, et envoie une `Notification` système si l'onglet est caché et la permission déjà accordée (`NotificationOptIn` sur `/arena` la demande sur un geste, jamais d'office). Aperçu : `/dev/screens?screen=arena-invite`. Une partie n'est visible que de ses joueurs (`loadFor` → `not_found`). Les joueurs sont toujours renvoyés dans un ordre stable (hôte d'abord). **Invitations croisées** : si A invite B et que B invite A, une seule salle doit exister, celle ouverte en premier, dont le créateur reste l'hôte. À la création (`createMatch`), si un invité héberge déjà une salle où l'hôte est `invited` (`lobbiesInvitingHostedBy`, la plus ancienne), l'hôte y passe `ready` et ses autres invités y sont amenés (`carryGuests` : ajout dans la limite de `maxPlayers`, saut sur conflit de marqueur, un `invited` déjà `ready` ailleurs devient `ready`) au lieu d'ouvrir une seconde salle. Pour deux salles déjà croisées (course, anciennes données), `mergeReciprocal` (appelé par `settle` pour toute salle lue, et par `listArenaInvites` pour les salles hébergées par le lecteur) ferme la plus récente par `UPDATE` conditionnel (`status = cancelled`, événement `merged { into }`), fait passer son hôte `ready` dans l'ancienne et y amène ses invités (événement `merge`). L'instantané d'une salle ainsi fermée porte `match.mergedInto` et `ArenaMatch` redirige vers la salle conservée (`router.replace`).
- **Le serveur arbitre, les téléphones jugent la géométrie** : seule la caméra sait où sont les feuilles, donc `hit` (œuf) et `bonusIds` (langue) sont décidés par le téléphone du tireur et envoyés à `POST /api/arena/:id/action` ; le serveur n'impose que la cadence, les dégâts, le plafond de soin, la fin et les rangs. Ne pas essayer de revérifier la géométrie côté serveur.
- Synchronisation : interface `ArenaTransport` (`components/arena/transport.ts` : `subscribe`, `act`, `shoot`, `lick`, `linkState`, `broadcast`, `subscribePeers`). `PollingTransport` interroge `GET /api/arena/:id?since=<curseur>` toutes les `ARENA.pollMs` (500 ms) en bataille, `lobbyPollMs` en salle, et s'arrête à la fin ; sans `since` = instantané complet (`players[].creature` dessinable, pas d'événements), avec `since` = seulement les événements nouveaux (`completeSnapshot` recopie les créatures du précédent). `createArenaTransport(matchId, initial, { webrtc, userId })` : case admin décochée (ou pas de `RTCPeerConnection`) = sondage seul ; cochée = `LazyRtcTransport` charge `rtc-transport.ts` à la demande et empile `RtcTransport` sur un sondage ralenti à `ARENA.rtc.pollMs` (1 s). `PreviewTransport` (`preview-transport.ts`) = arbitre en mémoire (Léa tire, lèche, la partie se termine) pour `/dev/screens?screen=arena`.
- **Liaison directe WebRTC** (`RtcTransport`) : un canal de données par paire (maillage complet, 2 à 4 joueurs), ouvert par des signaux relayés par le serveur (`POST` / `GET /api/arena/:id/signals?since=`, événements `arena_events.kind = 'signal'` adressés à un joueur via `payload.to`, **jamais** renvoyés dans les instantanés, `postSignal` / `listSignals`), sans trickle ICE (offre et réponse attendent les candidats, `ARENA.rtc.gatherTimeoutMs`), STUN publics (`ARENA.rtc.iceServers`) plus, quand `CLOUDFLARE_TURN_KEY_ID` / `CLOUDFLARE_TURN_API_TOKEN` sont définies, le **relais TURN de Cloudflare** : `lib/arena/turn.ts` (`getTurnIceServers()`, `POST …/credentials/generate-ice-servers` avec `ttl` = `TURN_TTL_SECONDS`, réponse `iceServers` en tableau ou objet via `parseIceServers`, cache serveur pour la moitié de la durée, `null` sans variables ou sur erreur, `fetch` injectable pour les tests, jeton jamais exposé), servi par `GET /api/arena/:id/ice` aux joueurs `ready` d'une partie ouverte (`assertArenaPlayer`) ; `HttpSignaling.iceServers()` le récupère au démarrage de `RtcTransport` (`prepare`, attente bornée à 4 s) et l'ajoute aux STUN, `hasRelay` le signale ; `getConfigStatus().turn` l'affiche dans Plus → état des services. Une paire injoignable reste sur le sondage, sans erreur. L'ouvreur d'une paire est le plus petit `userId` (`isOfferer`, `lib/arena/rtc-protocol.ts`) ; chaque onglet a une `session` (nonce) : le non-ouvreur envoie `hello` au démarrage et tant qu'il n'est pas relié, l'ouvreur (ré)offre sur un `hello` d'une session inconnue ou d'une tentative bloquée, une réponse n'est acceptée que si `target` = la session de l'offre. Messages du canal (`PeerMessage`, `parsePeerMessage` : ne jamais faire confiance au pair) : `egg` (au lancer, point visé dans le repère de la cible), `hit` (verdict et point réel à l'atterrissage), `tongue` (au départ), `eat` (bons aliments attrapés). Chaque tir et coup de langue porte un `nonce` envoyé aussi au serveur (`ShotInput.nonce`, `TongueInput.nonce`, recopié dans l'événement) : `NonceMemory` dans `ArenaGame` dessine chaque œuf et chaque langue une seule fois, quel que soit le chemin arrivé en premier (`hit:` et `eat:` sont mémorisés à part) ; un œuf reçu sans verdict attend `VERDICT_GRACE_SECONDS` sur son point de chute avant d'être jugé localement ; les points de vie baissent (ou remontent) de façon optimiste sur le message direct, l'arbitre confirme au sondage suivant. La signalisation ne démarre que lorsque le joueur du téléphone est `ready` (`syncPeers`) ; une fois le canal ouvert, `inspect` lit par `getStats()` le chemin de la paire retenue (`LinkPeer.via` : `host` même réseau, `srflx` STUN, `relay` TURN, `selectedPairKind`). HUD : pastille « Direct n/m · relais | local | connexion… | sondage » (`linkLabel`, `data-link`, `data-peers`, `data-link-label`, détail par pair dans `title`). `ChannelSignaling` (signaux par `BroadcastChannel`) et `/dev/screens?screen=arena-rtc&as=me|lea` : deux onglets reliés pour de vrai, chacun avec son arbitre en mémoire (les points de vie ne sont pas partagés, seuls les œufs et les langues traversent). Le test e2e lance Chromium avec `--disable-background-timer-throttling --disable-renderer-backgrounding --disable-backgrounding-occluded-windows` ; l'onglet non focalisé garde malgré tout un `requestAnimationFrame` ralenti.
- Client : `lib/game/arena-local.ts` (pur, testé : œufs en vol, langues, effets, cadences ; chaque objet porte le `marker` dont il suit le repère), `lib/game/arena.ts` (pur, testé : `rankArenaPlayers`, `arenaScore`, `placeArenaBonus`, `arenaAimTarget` = adversaire le plus proche du point visé sous `aimMaxRadius`, touché sous `hitRadius`), `ThreeStage.worldPoint` / `localPoint` / `topOf` / `isVisible` (passage d'un repère de marqueur à l'autre), `components/ar/three/arena-scene.ts` (`ArenaScene` : une couche par marqueur attachée au slot du stage — barre de vie au-dessus des adversaires, langue, bons aliments ancrés, œufs dans le repère de leur `marker`, effets ; props partagés avec « Défendre » dans `props.ts` : `EffectPool`, `TongueProp`, `HealthBarPool`), `components/arena/arena-game.tsx` (`MarkerCamera`, `ensureTarget` pour chaque marqueur de joueur aperçu, visée = `arenaAimTarget` sur `aimOnMarker` de chaque adversaire visible ; **œuf** simulé dans le repère de sa propre feuille quand elle est visible — la cible peut esquiver en déplaçant la sienne, le coup est jugé à l'atterrissage dans le repère actuel de la cible (`judgeLanding`) — sinon dans celui de la cible depuis le téléphone ; **langue** dans son propre repère, bons aliments convertis depuis leur marqueur d'ancrage (`resolveTongue`, `sweptByTongue`) ; rejeu des œufs et langues des autres depuis les événements (`replayEvents`, la créature de l'auteur se tourne) ; HUD `data-arena-hud` (temps, pv, adversaires, cible) mis à jour au changement seulement ; boutons `data-arena-fire` / `data-arena-tongue` (espace / entrée, L) ; pause quand l'onglet est caché ; `preview` expose `window.__arenaScene` et `window.__arenaLocal`), `components/arena/arena-match.tsx` (salle d'attente, jeu, classement et récompense ; ne se re-rend pas pendant la bataille), `components/arena/arena-list.tsx` (invitations, partie en cours, création avec choix des amis, dernières batailles). Pages `/arena` et `/arena/[id]` (avec la `MarkerCard`) ; entrées : rangée « Voir en vrai · Arène » de l'écran Créature, carte sur « Voir en vrai », menu Plus.
- Test sans caméra : `/dev/screens?screen=arena` (Miso n° 17 = soi, Pipo n° 42 = Léa, arbitre en mémoire) ; fausse caméra avec les deux marqueurs en perspective, en incluant les SVG des `#dev-marker-*` tels quels (une capture d'élément peut être recouverte par une barre collante). Sous `page.clock`, `performance.now()` peut reculer : le pas de simulation est borné à 0.

## Mises de l'Arène (accessoires pariés)

- Dans les modes à vainqueur (`STAKE_MODES` = `arena`, `pingpong` ; jamais en coop), chaque joueur `ready` d'une salle peut miser des exemplaires d'accessoires de sa collection, ou rien : table `arena_stakes` (migration 017, une ligne par exemplaire `(match_id, user_id, accessory_id)` unique, `taken_at` = séquestre, `settled_at` / `winner_id` = règlement) et `arena_players.stakes_agreed_at`. Service : `setStake(userId, matchId, accessoryId, staked)` (salle seulement, accessoire possédé, `no_stakes` hors modes à mises) et `agreeStakes()` ; **toute modification du pot remet à zéro la validation de tous** (`resetAgreements`), un joueur qui quitte la salle emporte ses mises (`removeStakesOf`). `startMatch` refuse tant qu'un joueur `ready` n'a pas validé (`stakes_not_agreed`, même sans aucune mise), puis **séquestre** les exemplaires (`escrowStakes` : `taken_at` posé par `UPDATE` conditionnel, puis `decrementAccessoryCopy()` de `lib/accessories/service.ts`, qui laisse la ligne à 0 et **ne déshabille pas** la créature pendant la partie ; un exemplaire disparu entre-temps quitte le pot, événement `stake_dropped`). Règlement paresseux et idempotent `settleStakes()` (appelé par `settle()` sur toute partie `finished` / `cancelled`, après `finishIfOver` et par `finishPingPong`) : l'unique 1er (`stakeWinner`, rangs de `rankArenaPlayers` ou du ping-pong) reçoit chaque exemplaire par `addAccessoryCopies()` et le perdant passe par `dropEmptyAccessory()` (ligne à 0 supprimée, accessoire retiré de la tenue) ; égalité au premier rang ou partie annulée = chacun récupère le sien ; quitter la bataille = perdre sa mise. Vues : `ArenaMatchView.stakes { enabled, agreed, waitingFor, pot, winnerId }`, `ArenaPlayerView.stakes[] / agreed`, `ArenaSnapshot.collection` (instantané complet d'une salle : ce que le lecteur possède, avec `equipped`). Actions `POST /api/arena/:id/action { action: "stake", accessoryId, staked }` et `{ action: "agree" }` ; transport `act(move: LobbyMove)`. UI : `components/arena/stakes-card.tsx` (`StakesCard` dans la salle : mises par joueur, badge « A validé », collection en cases à cocher, bouton `data-arena-agree` ; `StakesResult` sur le bilan : `data-outcome` ∈ `won | lost | none | tie`), bouton de lancement « En attente des validations… ». `PreviewTransport` : Léa mise un nœud papillon et valide 1,5 s après le lecteur (`/dev/screens?screen=arena`).

## Défendre ensemble (ex-« Défendre à deux », défense coopérative sur la table)

- Même infrastructure que l'Arène (salle, invitations, invitations croisées, transport, WebRTC, récompenses) : une partie porte `arena_matches.mode` ∈ `arena | coop` (migration 016, avec `seed` = graine des vagues, `state` jsonb = `{ live?: CoopStateMessage, result?: { score, perfect, summaries } }` et `state_at`). `createMatch(…, { mode })`, `ArenaMatchView.mode / seed / defense (règles Défendre du moment) / coop { live, liveAt, result }`, `ArenaInviteNotice.mode` ; les salles croisées ne sont réunies qu'entre parties **du même mode**. `startMatch` en coop : `ends_at` = `COOP.maxSeconds` (filet), pas de bons aliments d'arène (`tickBonuses` sauté) ; `finishIfOver` ne ferme qu'à l'échéance ; `finishCoop(userId, matchId, summaries)` (tout joueur `ready`, premier arrivé par `UPDATE` conditionnel) calcule `coopScore` (moyenne des `computeDefenseScore` par créature, parfait si toutes le sont), range tout le monde 1er et stocke `result` ; `rewardIfDue` en coop = ce score pour chaque joueur `ready` (`recordPlay(…, "coop")`), `skipped: unfinished` sans bilan ; `storeCoopState` (hôte seul, `parseCoopState`) et `logCoopEvent` (`fire | smash | lick | catch`) pour les téléphones qui sondent. Actions de `POST /api/arena/:id/action` : `state`, `fire`, `smash`, `lick`, `catch`, `finish { summaries }` ; `POST /api/arena { mode }`.
- **Simulation partagée** : chaque téléphone fait tourner **une `DefenseState` par créature** (`lib/game/defense.ts`, repère du papier de cette créature) avec un générateur à état (`lib/game/random.ts`, `coopRandom(seed, rang du joueur)`, joueurs dans l'ordre stable hôte d'abord). Le téléphone de l'hôte fait foi : il publie `{ hostTime, states: { [userId]: { state: serializeDefense(), rng } } }` toutes les `COOP.broadcastMs` sur la liaison directe (`PeerMessage` `state`) et toutes les `COOP.storeMs` au serveur ; un invité **prédit** localement puis adopte l'état de l'hôte à chaque réception (`adoptHostState` : conserve ses propres œufs et langue en vol, ses rechargements et son cap, restaure le générateur, rattrape la latence par `advanceDefense`, bornée par `COOP.maxCatchUpSeconds`). Les œufs et langues des partenaires sont **cosmétiques** (`fireDefense(…, { cosmetic, from })`, `tongueDefense(…, { cosmetic })` : dessinés, sans rechargement, sans effet) ; ce qu'ils touchent ou avalent arrive par leurs rapports : `state.recentSmashes` / `recentCatches` (remplis par la simulation à l'atterrissage et à pleine extension, bornés, vidés par le jeu) → messages `smash { frame, hits, x, y }` et `catch { frame, bonusIds, junkIds }` appliqués par `smashDefense` / `applyCatch` sur tous les téléphones (l'hôte fait foi ; ids d'ennemis stables car les apparitions sont déterministes). Le tir vise le papier le plus proche du réticule (`COOP.aimMaxRadius`) : l'œuf part de la créature du tireur (bouche convertie dans le repère de la cible par `ThreeStage.worldPoint / localPoint`) ; la langue ne joue que dans son propre repère. Le serveur n'est sollicité pour `fire` / `smash` / `lick` / `catch` que si la liaison directe n'atteint pas tout le monde (`relayNeeded`). Fin : quand toutes les créatures sont K.-O. (l'hôte envoie `finish`), sur « Terminer » de l'hôte, ou par un invité si l'hôte se tait plus de `COOP.hostSilenceMs` (« Terminer la partie » avec son propre bilan). L'hôte doit garder l'écran allumé : sa pause fige la partie.
- Client : `components/coop/coop-game.tsx` (`CoopGame`, une `DefenseScene` par marqueur attachée au slot du stage, HUD `data-coop-hud` avec `data-creatures`, boutons `data-coop-fire` / `data-coop-tongue`, `preview` expose `window.__coopSims`), `ArenaMatch` rend `CoopGame` quand `mode = coop` (titres et bilan d'équipe `data-coop-results`), `ArenaList` propose le mode à la création (`data-arena-mode`), `live-arena` adapte le texte de l'invitation ; `components/arena/link-label.ts` partagé. Aperçu : `/dev/screens?screen=coop` (hôte, arbitre en mémoire : `PreviewTransport.coop()` garde l'état et clôt sur `finish`). Nouveau réglage : `COOP` dans `config.ts`.

## Ping-pong (duel en réalité augmentée)

- Mode `pingpong` des parties entre amis (migration 018 : `arena_matches.mode` accepte `pingpong`, `arena_players.points`, `play_sessions.kind` accepte `pingpong`), **exactement deux joueurs** (`createMatch` : un seul invité, `too_many` ; `startMatch` : deux `ready`). Même infrastructure que l'Arène et « Défendre à deux » (salle, invitations, mises, transport, WebRTC, `HOSTED_MODES` = `coop | pingpong` pour `storeMatchState` (mode-aware : `parsePingPongState` / `parseCoopState`), `logMatchEvent` (`MATCH_EVENT_KINDS` += `swing | serve`), échéance `PINGPONG.maxSeconds`). Logique pure `lib/game/pingpong.ts` (test à côté), temps en ms sur l'horloge du serveur (`transport.serverNow()`) : `createPingPong([hôte, invité], now, endsAt)`, `servePingPong` (le serveur du point seul, phase `serve`, service automatique après `serveTimeoutMs`), `hitPingPong(state, userId, at, shot)` (le receveur seul ; fenêtres `hitWindows(flight, rules)` = `goodWindowPercent` / `perfectWindowPercent` du **temps de vol** de la balle, planchers `PINGPONG.minGoodMs` / `minPerfectMs`, donc plus l'échange est rapide, plus il faut être précis ; dans la fenêtre, la balle repart depuis l'**heure d'arrivée** avec `Flight.kind` : `smash` si parfait (rythme +2, vol × `smashFactor`, plancher `smashMinMs`), `lob` si `shot = "lob"` (vol × `lobFactor`, quel que soit le timing), sinon `normal` (rythme +1) ; `flightMs(pace, rules, kind)` = `firstFlightMs × paceFactor^pace` borné par `minFlightMs` ; trop tôt = point à l'envoyeur (`early`), trop tard = ignoré), `timingRingShown(state, now)` (faux au-delà de `ringHideAfterHits` renvois dans l'échange, et sur la dernière part `lobHiddenTail` d'un lob), `tickPingPong(state, now, { graceFor })` (service automatique, **raté** quand personne n'a frappé après la fenêtre + le délai de grâce accordé au receveur (`graceMs` pour l'autre téléphone, 0 pour soi), pause `pointPauseMs` puis service du perdant, fin au clock), `endPingPong` (le meneur gagne, égalité possible), `concedePingPong` (un joueur parti), `ballProgress` / `inHitWindow` (dessin), `pingpongScore(mien, sien, gagné)` (moitié part des points, moitié victoire (un quart à égalité), parfait = victoire sans point concédé), `state.seq` (transition = +1), `state.recent` (notes visuelles, `shot` sur les frappes), `serializePingPong` / `parsePingPongState` (tolère un état sans `rules` ni `kind`, venu d'un ancien téléphone). **Règles admin** `rules.pingpong` (`PingPongRules`, section « Ping-pong » de `/admin` : points, vol de départ, plancher, facteur, fenêtres en %, anneau caché après N renvois, facteurs lob / smash) : servies dans `ArenaMatchView.pingpong.rules`, **copiées dans l'état** à sa création (`createPingPong(players, now, endsAt, rules)`, `state.rules`) et donc publiées par l'hôte, pour que les deux téléphones jugent avec les mêmes nombres. Constantes fixes dans `PINGPONG` (`config.ts`).
- **L'hôte arbitre, chaque téléphone juge sa propre frappe** : le receveur juge le moment de sa frappe sur son horloge et l'envoie (`swing { flightId, at, shot }` avec `shot` ∈ `normal | lob`, `serve { at }`, message direct ou relais serveur si la liaison n'atteint pas l'autre), l'hôte l'applique avec ce `at` (borné à `REPORT_MAX_AGE_MS` / `REPORT_MAX_AHEAD_MS`) : la latence ne ralentit jamais l'échange, seul un rapport plus tardif que `graceMs` devient un raté. L'hôte publie son état (`ppstate` sur la liaison directe toutes les `broadcastMs`, action `state` au serveur toutes les `storeMs`) ; l'invité prédit sa propre frappe et n'adopte l'état de l'hôte que si `host.seq ≥ local.seq` (ou après `maxPredictionMs`). Fin : `finishPingPong(userId, matchId, { points, longestRally, hits, perfects })` (tout joueur `ready`, premier arrivé) → `result { points, winnerId, … }` dans `state`, `arena_players.points`, rangs 1 / 2 (1 / 1 à égalité), `stakes` réglées ; l'hôte envoie `finish` à la fin (`over`), l'invité quand l'hôte a quitté la partie ou, à la main, après `hostSilenceMs`. Récompense `recordPlay(…, "pingpong")` avec `pingpongScore`, `skipped: unfinished` sans score.
- Client : `components/pingpong/pingpong-game.tsx` (`PingPongGame` : caméra, les deux créatures tournées l'une vers l'autre, HUD `data-pingpong-hud` (`data-phase`, `data-points`, `data-incoming`, `data-serving`), invite `data-pingpong-prompt`, retour `data-pingpong-feedback`, bouton `data-pingpong-hit` « Servir / Frapper » (espace / entrée) et bouton `data-pingpong-lob` « Lob » (touche L) visible quand la balle arrive, retours « Smash ! » / « Lob ! » / « Bien joué », invites qui annoncent un smash ou un lob adverse et l'absence d'anneau, `preview` expose `window.__pingpong` (état + `me`), `__pingpongScene`, `__pingpongStage` et fait jouer Léa (`autoplayOther`, un lob tous les cinq renvois)), `components/ar/three/pingpong-scene.ts` (`PingPongScene` : balle et ombre **dans le repère de la caméra** (`ThreeStage.attachWorld`) sur un arc entre les raquettes (`handPoint` : à côté de la créature, vers l'autre feuille ; arc × 1,8 pour un lob, × 0,45 pour un smash, `ARC_BY_KIND`), anneau de timing sur la feuille du receveur qui se resserre et **devient vert dans la fenêtre** (visible seulement quand `timingRingShown`), double éclat sur un smash, raquette par créature qui balaie à la frappe, effets ; `note(effect)` consomme `state.recent`), `ArenaMatch` (titres, `data-pingpong-results` : score, plus long échange, renvois / parfaits, récompense), `ArenaList` (mode « Ping-pong », un seul ami sélectionnable), notices en direct. Aperçu : `/dev/screens?screen=pingpong` (Miso n° 17 contre Pipo n° 42 joué par le téléphone, arbitre en mémoire). Remarque test : sous `page.clock`, les captures d'écran ne suivent pas l'état ; sonder `__pingpongScene.ball.position` en temps réel.

## Nourrissage et IA

- Toute lecture d'une créature vivante passe par `getActiveCreatureTicked()` (tick paresseux). Ne jamais lire `creatures` directement pour afficher des stats.
- `feedCreature()` reçoit `analyzer` et `storage` injectés : les tests utilisent un faux analyseur et un stockage en mémoire (`lib/meals/meals.integration.test.ts`). Le vrai couple est `analyzeMealWithGemini` + `r2Storage`. **Tout part en même temps** : l'analyse IA et l'envoi de la photo démarrent dès la réception, pendant que tournent les vérifications (créature, limite du jour, doublon, ces deux dernières en parallèle) ; un refus (`no_creature`, `meal_limit`, `duplicate_meal`, `not_food`, `screen_photo`, erreur IA) supprime la photo déjà envoyée (`discard` → `storage.remove`), rien ne reste en stockage. `FeedResult.timings` (`checks`, `analysis`, `upload`, `persist`, en ms depuis le début) est renvoyé par `POST /api/meals` dans l'en-tête **`Server-Timing`** (avec `session`, `body`, `total`) : le lire dans l'onglet Réseau pour savoir où part le temps. `HEAD /api/meals` réchauffe la fonction (instance, `select 1` Neon, choix du modèle Gemini via `warmGemini()`, plafonné à 2,5 s) : `FeedFlow` l'appelle à l'ouverture de l'écran et à la prise de la photo (`warmAnalysis()`). Photo réduite à `FEEDING.resizeMaxPx` = 768 px côté client (un seul carreau d'image pour Gemini). `vercel.json` fixe la région des fonctions à `fra1` (Francfort), à côté de Neon.
- Le prompt Gemini vit dans `lib/ai/meal-prompt.ts` ; le contrat JSON dans `lib/ai/meal-schema.ts` (zod, normalisation tolérante). Toujours garder les deux alignés (y compris `photo_source`, qui alimente `meals.photo_source` et la règle admin `feeding.rejectScreenPhotos`). `lib/ai/gemini.ts` : la **réflexion est coupée** (`THINKING_LADDER` : `thinkingBudget: 0` (Gemini 2.5), puis `thinkingLevel: "minimal"`, `"low"` (Gemini 3), puis rien ; un HTTP 400 fait passer au cran suivant, le cran accepté est mémorisé par modèle et par instance, `resetGeminiMemoForTests()` dans les tests) ; sans `GEMINI_MODEL`, la liste des modèles est demandée avec un délai borné (`LIST_TIMEOUT_MS`, chaîne statique au-delà), partagée entre appels simultanés, mise en cache 1 h (5 min après un échec) ; avec `GEMINI_MODEL`, aucune liste n'est demandée. Test `lib/ai/gemini.test.ts` avec un `fetch` simulé.
- Prise de photo : un seul `<input capture="environment">`, pas de bouton « galerie » (friction anti-triche voulue ; sur ordinateur le navigateur affiche quand même un sélecteur de fichiers, la vraie protection est `photo_source` + la règle admin).
- Les photos ne sont jamais servies en direct : `signedUrl()` (1 h) à chaque lecture.
- Animation de nourrissage (`components/game/feed-animation.tsx`, étape `serving` de `FeedFlow` entre l'analyse et le résultat) : les aliments renvoyés par Gemini sont classés en dessins par `foodKindsFor()` (`lib/meals/food-icons.ts`, mots-clés français, test à côté) et dessinés par `FoodIcon` (`components/food/food-icon.tsx`, viewBox 48×48). Chronologie fixe (apparition → lancer → repas → note), une copie de chaque aliment vole vers chaque créature nourrie (`others` de `POST /api/meals` porte `speciesId`, `stageId`, `state`, `healthBefore/After`), bouton « Passer », sautée sous `prefers-reduced-motion`. Nouveau type d'aliment : ajouter le mot-clé dans `RULES`, le dessin dans `DRAWINGS` et le libellé dans `FOOD_KIND_LABELS`.

## Points d'attention

- Vercel Hobby : pas de WebSocket, pas de cron fiable → toute logique temporelle est calculée paresseusement à la lecture (`lib/game/tick.ts`).
- Ne jamais lancer `pkill -f "next dev"` depuis une commande dont la ligne contient elle-même « next dev » (elle se tuerait) : utiliser un motif avec crochets (`"next de[v]"`) ou tuer par port.
- Better Auth utilise un `baseURL` dynamique (`*.vercel.app` autorisé) : ne pas définir `BETTER_AUTH_URL`.
- Neon HTTP ne supporte pas `db.transaction()` : utiliser `db.batch()` ou des mises à jour conditionnelles.
- Les photos de repas sont privées : bucket R2 privé, presigned GET courts, jamais exposées aux amis (seul le coach actif d'un joueur les voit, voir « Coaching »).
